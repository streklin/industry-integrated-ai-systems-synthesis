# ── Core scientific / data stack ──────────────────────────────────────────────
numpy>=2.4.0
pandas>=2.2.3
matplotlib>=3.10.0
seaborn>=0.13.2
tqdm>=4.67.0
scikit-learn>=1.7.0

# ── Image / video ──────────────────────────────────────────────────────────────
Pillow>=12.1.0
imageio>=2.37.0
imageio-ffmpeg>=0.6.0          # required by imageio for MP4/H.264 output

# ── Simulation / visualisation ─────────────────────────────────────────────────
pygame>=2.6.1
perlin-noise>=1.13

# ── Deep learning ─────────────────────────────────────────────────────────────
# CPU-only build. For GPU support install torch/torchvision from:
#   https://pytorch.org/get-started/locally/
# e.g. pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
torch>=2.0.0
torchvision>=0.15.0
segmentation-models-pytorch>=0.5.0

# ── Agentic workflow ───────────────────────────────────────────────────────────
pydantic>=2.12.0
pydantic-ai>=1.103.0
anthropic>=0.104.0
python-dotenv>=1.2.0
mgraph-db>=1.18.0
osbot-utils>=3.74.0
